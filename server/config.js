module.exports = {
  PORT: parseInt(process.env.PORT, 10) || 3001,
  HOST: process.env.HOST || '127.0.0.1',

  // Gemini backend selector: 'direct' (default, API key) or 'vertex' (GCP ADC)
  GEMINI_BACKEND: process.env.GEMINI_BACKEND || 'direct',

  // Vertex AI config (only used when GEMINI_BACKEND=vertex)
  VERTEX_PROJECT: process.env.GOOGLE_CLOUD_PROJECT || process.env.VERTEX_PROJECT || '',
  VERTEX_LOCATION: process.env.GOOGLE_CLOUD_LOCATION || process.env.VERTEX_LOCATION || 'global',

  OPENROUTER_API_KEY: process.env.OPENROUTER_API_KEY || '',
  OPENROUTER_VISION_MODEL: process.env.OPENROUTER_VISION_MODEL || 'qwen/qwen2.5-vl-72b-instruct:free',

  JSON_BODY_LIMIT: '20mb',
  SHUTDOWN_TIMEOUT_MS: 10_000,

  GEMINI_GENERATE_TIMEOUT_MS: 180_000,
  GEMINI_TEXT_TIMEOUT_MS: 120_000,
  ROUTE_TIMEOUT_MS: 5 * 60_000,
  HEALTH_CHECK_TIMEOUT_MS: 12_000,
  IMAGE_DOWNLOAD_TIMEOUT_MS: 60_000,
  HTML_FETCH_TIMEOUT_MS: 45_000,
  VIDEO_DOWNLOAD_TIMEOUT_MS: 120_000,
  FFMPEG_TIMEOUT_MS: 30_000,
  SLOW_REQUEST_THRESHOLD_MS: 10_000,

  GEMINI_TRANSIENT_RETRIES: 5,
  GEMINI_TRANSIENT_BASE_MS: 2000,
  GEMINI_EMPTY_RESPONSE_MAX_ATTEMPTS: 3,
  GEMINI_EMPTY_RESPONSE_BASE_MS: 250,
  IMAGE_DOWNLOAD_MAX_ATTEMPTS: 3,
  IMAGE_DOWNLOAD_BACKOFF_BASE_MS: 500,

  MAX_REFERENCE_BYTES: 10 * 1024 * 1024,
  MAX_UPLOAD_BYTES: 10 * 1024 * 1024,
  MAX_MULTIPART_BYTES: 50 * 1024 * 1024,
  MAX_VIDEO_UPLOAD_BYTES: 200 * 1024 * 1024,
  MAX_STORE_BYTES: 500 * 1024 * 1024,

  BATCH_MAX_CONCURRENCY: 5,
  BATCH_MAX_SIZE: 20,
  BATCH_MAX_RUNNING_JOBS: 10,
  MAX_BATCH_PROMPTS: 20,
  MAX_AUTO_DURATION_DAYS: 30,

  IMAGE_TTL_MS: 60 * 60 * 1000,
  IMAGE_CLEANUP_INTERVAL_MS: 10 * 60 * 1000,
  BATCH_JOB_TTL_MS: 7 * 24 * 60 * 60 * 1000,
  BATCH_CLEANUP_INTERVAL_MS: 5 * 60 * 1000,
  HEALTH_CACHE_TTL_MS: 30_000,
  AVAILABILITY_CACHE_TTL_MS: 60_000,
  STALE_TEMP_FILE_AGE_MS: 60 * 60 * 1000,

  PROMPT_MAX_LENGTH: 15_000,
  API_KEY_MAX_LENGTH: 200,
  MAX_CAROUSEL_SLIDES: 10,
  MAX_HASHTAGS: 25,
  DEFAULT_HASHTAG_COUNT: 15,

  GEMINI_CLIENT_CACHE_SIZE: 5,
  AVAILABILITY_CACHE_MAX_SIZE: 100,

  COMPRESSION_MIN_BYTES: 1024,
};
