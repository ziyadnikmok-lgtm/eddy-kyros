# Dependencies
node_modules/

# Build output
# client/dist/ — tracked so Dokploy serves without a build step
scripts/seed-data/

# Environment — never commit real secrets
.env
.env.*
!.env.example
build/.env

# Encrypted keys
server/data/keys.enc

# User data (gallery, characters, uploads)
server/data/gallery.json
server/data/analyzedProfiles.json
server/data/sceneMemory.json
server/data/outfits.json
server/data/brandVoice.json
server/data/backgrounds/
server/data/ig-debug/
uploads/generated/
uploads/Maria/
uploads/pdf_extracts/
uploads/sanitized/
characters/

# Temp files
temp/

# Root-level data folder (user content)
data/

# Zip archive
*.zip

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/

# Logs
*.log
npm-debug.log*
.pnpm-store
.gstack/
server/backup-prompt-engine-v1/
server/backup-prompt-engine-v2/
dist-electron/
userdata/

# Local backup of AppData — contains real secrets and encrypted API keys.
_appdata_backup/

# Leftover from a git filter-branch. Not source, and it is thousands of files.
.git-rewrite/
