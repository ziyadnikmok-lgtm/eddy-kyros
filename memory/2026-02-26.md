# 2026-02-26

- User preference: whenever I learn something useful/new, I should write it down to memory so it persists.
- Memory policy agreed: store operational learnings in daily memory notes and durable preferences in MEMORY.md.
- User plans to install Togglex so browser visibility/control is available.
- Completed task today: installed ClamAV (`clamscan`), verified version 1.5.1.
- Inspected ClawHub skill `toggle` source before install: no obvious malware patterns; primary risk is external API/data-sharing by design.
- Installed ClawHub skill `toggle` with `--force`; installed version 1.0.6 at `skills/toggle`.
- Configured Toggle skill API key in `~/.openclaw/openclaw.json` under `skills.entries.toggle.apiKey` (value redacted in notes).
- Browser control note: current OpenClaw browser relay profile available is Chrome (not Edge) in this setup.
- Retested Toggle fetch after setup: API works (`exit 0`) but initially returned zero workflows for today.
- After user installed/connected Toggle in Chrome, Toggle API now returns data (`totalWorkflows: 1`, `totalDays: 1`) for today.
- Browser relay troubleshooting: gateway logs showed repeated `invalid request frame` from Chrome extension origin, indicating extension/gateway protocol mismatch. Installed current relay bundle via `openclaw browser extension install`; user must load unpacked extension from `~/.openclaw/browser/chrome-extension`.
- Reliable workaround discovered: use managed browser profile `openclaw` directly. Verified by opening `https://x.com` and capturing a live snapshot successfully (tabs + feed/trending visible).
- Durable troubleshooting workflow added to MEMORY.md: check memory first, verify gateway service health, avoid relay retry loops on handshake mismatch, use `openclaw` profile fallback.
- Added durable X posting workflow to MEMORY.md (openclaw profile, upload via inputRef, fallback evaluate click for Post, verify sent toast + status URL).
- Created 10/day automation with 10-minute spacing using OpenClaw cron jobs (`x-of-reply-burst-1`, `x-of-reply-burst-2`) and switched timezone to `America/New_York`.
- Added config-driven tracking: `/Users/admin/.openclaw/workspace/social-reply-config.json` (image folder + tracked profiles + mode).
- Updated cron payloads to read config/state and reply only on new tracked-profile posts.
- Rebuilt dashboard at `/Users/admin/.openclaw/workspace/dashboard.html` with clearer UI: simple form fields (timezone, runs/day, spacing, image folder, tracked profiles), step-by-step actions, config preview, and copy-save command.
- Added direct image upload to dashboard by replacing static server with `/Users/admin/.openclaw/workspace/dashboard_server.py` (serves dashboard + upload/list APIs); images now upload straight into `/Users/admin/Downloads/IG POST SOFIA`.
- Added one-click “Instant reply to specific X link” in dashboard: `/api/reply-now` queues URL in `social-reply-queue.json` and triggers cron job immediately; dashboard also shows queue status.
- Fixed dashboard outage: Python runtime lacked deprecated `cgi` module; migrated upload API to JSON+base64 and updated frontend uploader accordingly.
- Added queue status sync in dashboard backend: `/api/list-queue` now enriches queue items from `openclaw cron runs` summaries and shows reply URL when available.
- Updated automation style: short, flirty, confident, engaging replies with hot vibe but non-explicit language; image always selected from configured folder.
- Dashboard upgraded with grouped sections + queue delete + optional image selection for instant reply URL.
- Added Viral Session control: one-click starts 2-hour sprint (every 5 min) via temporary cron job auto-removed after 2h.
- Verified dashboard Start button works end-to-end (returns jobId and active state); added button loading/disabled UI states to make click feedback obvious.
- Fixed instant-reply reliability UX: accepts x.com/twitter.com links, shows busy queue messaging, and switched cron delivery mode to `none` to avoid channel-required errors during manual triggers.
- Searched ClawHub for dashboard helpers (`task-monitor`, `openclaw-dashboard`) and inspected code before install; did not install since current custom dashboard already covers required queue/reply controls.
