# 2026-02-26

- User preference: whenever I learn something useful/new, I should write it down to memory so it persists.
- Memory policy agreed: store operational learnings in daily memory notes and durable preferences in MEMORY.md.
- User plans to install Togglex so browser visibility/control is available.
- Completed task today: installed ClamAV (`clamscan`), verified version 1.5.1.
- Inspected ClawHub skill `toggle` source before install: no obvious malware patterns; primary risk is external API/data-sharing by design.
- Installed ClawHub skill `toggle` with `--force`; installed version 1.0.6 at `skills/toggle`.
- Configured Toggle skill API key in `~/.openclaw/openclaw.json` under `skills.entries.toggle.apiKey` (value redacted in notes).
- Browser control note: current OpenClaw browser relay profile available is Chrome (not Edge) in this setup.
- Retested Toggle fetch after setup: API works (`exit 0`) but initially returned zero workflows for today.
- After user installed/connected Toggle in Chrome, Toggle API now returns data (`totalWorkflows: 1`, `totalDays: 1`) for today.
- Browser relay troubleshooting: gateway logs showed repeated `invalid request frame` from Chrome extension origin, indicating extension/gateway protocol mismatch. Installed current relay bundle via `openclaw browser extension install`; user must load unpacked extension from `~/.openclaw/browser/chrome-extension`.
- Reliable workaround discovered: use managed browser profile `openclaw` directly. Verified by opening `https://x.com` and capturing a live snapshot successfully (tabs + feed/trending visible).
- Durable troubleshooting workflow added to MEMORY.md: check memory first, verify gateway service health, avoid relay retry loops on handshake mismatch, use `openclaw` profile fallback.
- Added durable X posting workflow to MEMORY.md (openclaw profile, upload via inputRef, fallback evaluate click for Post, verify sent toast + status URL).
